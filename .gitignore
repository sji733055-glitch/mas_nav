# Python 缓存：colcon test / pytest 运行时生成
__pycache__/
*.pyc
.cache/

# colcon 构建产物。本仓库在宿主机 ROS 2 Jazzy 下直接 colcon，产物不要提交。
build/
install/
log/
data/

# 部署用的镜像包，不要提交
*.tar.gz
mas_nav_image.tar

# 排障用的一次性脚本 / 回放日志 / 台架输出。约定留在本地，不进仓库
# （见 docs/change-history.md 中多次写明的"一次性脚本都留在 .scratch/（不进仓库）"）。
.scratch/

# 给编辑器用的编译库软链（指向 build/compile_commands.json，由 src/tools/gen_compile_commands.sh
# 生成）。内容是绝对路径、随机器与构建变化，不进仓库；clangd 侧由 src/.clangd 指路。
/compile_commands.json
